var documenterSearchIndex = {"docs":
[{"category":"section","location":"performances/#Performance","page":"Performance","text":"KernelForge.jl achieves performance comparable to optimized CUDA C++ libraries such as CUB. Benchmarks report two metrics:\n\nKernel time: Execution time of the main kernel, measured using @profile from CUDA.jl\nOverhead: Total time minus kernel time, including memory allocations and data transfers","title":"Performance"},{"category":"section","location":"performances/#Copy","page":"Performance","text":"CUDA.jl leverages the proprietary libcuda library for memory copies, which internally vectorizes loads and stores. In contrast, the cross-platform GPUArrayCore.jl relies on KernelAbstractions.jl, which does not currently perform vectorization. KernelForge's vcopy! bridges this gap by using vload and vstore operations built on unsafe pointer access via LLVMPtrs from KernelIntrinsics.jl.\n\nThe graph below compares memory bandwidth for Float32 and UInt8 data types. With vectorized loads and stores, KernelForge achieves bandwidth comparable to CUDA.jl for both types. The slight underperformance below the L2 cache threshold stems from our current vectorization factor (×8 for Float32); increasing this to ×16 would close the remaining gap.\n\n(Image: Copy Bandwidth)","title":"Copy"},{"category":"section","location":"performances/#Map-Reduce","page":"Performance","text":"KernelForge.jl matches CUDA.jl performance on Float32 and significantly outperforms it on smaller types (UInt8, UnitFloat8), even when converting to Float32 during reduction. These gains result from optimized memory access patterns and vectorized loads/stores.\n\n(Image: Map-Reduce Benchmark)","title":"Map-Reduce"},{"category":"section","location":"performances/#Scan","page":"Performance","text":"KernelForge's scan kernel rivals CUB performance on Float32 and Float64, while additionally supporting non-commutative operations and custom types such as Quaternions. This is achieved through an efficient decoupled lookback algorithm combined with optimized memory access.\n\n(Image: Scan Benchmark)","title":"Scan"},{"category":"section","location":"performances/#Matrix-Vector-Operations","page":"Performance","text":"KernelForge implements matrix-vector and vector-matrix operations for general types and operators. For benchmarking, we compare against CUDA.jl on Float32, which internally calls cuBLAS's gemv routine.\n\nDue to column-major memory layout, matrix-vector and vector-matrix multiplications have fundamentally different access patterns. KernelForge therefore provides separate optimized kernels for each operation.\n\nFor both benchmarks, we fix the total matrix size (n × p) and vary n from 10 to (n × p) / 10, sweeping from tall-narrow to short-wide matrices. The black line indicates the reduced overhead achieved when the user provides pre-allocated temporary memory.\n\nMatrix-Vector (Image: Matrix-Vector Benchmark)\n\nVector-Matrix\n\n(Image: Vector-Matrix Benchmark)","title":"Matrix-Vector Operations"},{"category":"section","location":"performances/#Sort","page":"Performance","text":"KernelForge ships a 1D radix sort! competitive with CUB's DeviceRadixSort across the standard bitwise types (UInt32, UInt64, Float32, Float64). At large N (≥ 10⁷) KernelForge typically wins on narrow types and ties on wide types; at small N (< 10⁵) CUB pulls ahead because its launch overhead amortizes better. The keys=... keyval variant, sortperm!, and the batched sort_columns! family all reuse the same one-sweep radix kernel.\n\nFor workloads with custom comparators or non-radix bitstypes, sample_sort provides a general path; it currently matches CUDA.jl's bitonic sort on Float32 / Float64 at N ~ 10⁷ and remains correct (where bitonic doesn't) for arbitrary lt predicates.\n\nBenchmark scripts: perfs/julia/benchmarks/sort_perf_comparison.jl and perfs/julia/benchmarks/sort_columns_perf_comparison.jl. Raw result CSVs live in the sibling repo KernelForge-benchmarks; plot figures will land in a follow-up minor.","title":"Sort"},{"category":"section","location":"performances/#Random","page":"Performance","text":"The Philox4x32-10 RNG path matches Random123's CPU implementation bit-for-bit and outperforms CUDA.jl's cuRAND host-only launcher at small N because the Philox stream is fully GPU-resident — no host↔device round-trip. At large N (≥ 10⁷), Float32 Uniform draws saturate device memory bandwidth on RTX1000.\n\nrandperm! builds on the keyval sort path (fill Float32 uniform keys + sortperm!); throughput tracks sort! directly.\n\nBenchmark scripts: perfs/julia/benchmarks/random_perf_comparison.jl and perfs/julia/benchmarks/randperm_perf_comparison.jl. Raw CSVs in KernelForge-benchmarks.","title":"Random"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Copy","page":"API Reference","text":"","title":"Copy"},{"category":"section","location":"api/#Map-Reduce","page":"API Reference","text":"","title":"Map-Reduce"},{"category":"section","location":"api/#Scan","page":"API Reference","text":"","title":"Scan"},{"category":"section","location":"api/#Search","page":"API Reference","text":"","title":"Search"},{"category":"section","location":"api/#Matrix-Vector","page":"API Reference","text":"","title":"Matrix-Vector"},{"category":"section","location":"api/#Sort","page":"API Reference","text":"1D radix sort, permutation sort, general-comparator sample sort, and batched per-column sort. The sort1d! name from v0.1.x has been renamed to sort! — see the CHANGELOG for migration.","title":"Sort"},{"category":"section","location":"api/#Random","page":"API Reference","text":"Philox4x32-10 counter-based GPU RNG with parameterised distributions. Deterministic for a given seed across CPU and GPU.","title":"Random"},{"category":"section","location":"api/#Backend-introspection","page":"API Reference","text":"","title":"Backend introspection"},{"category":"section","location":"api/#Utilities","page":"API Reference","text":"","title":"Utilities"},{"category":"function","location":"api/#KernelForge.vcopy!","page":"API Reference","text":"vcopy!(dst::AbstractArray, src::AbstractArray; Nitem=4)\n\nCopy src to dst using vectorized GPU memory access.\n\nPerforms a high-throughput copy by loading and storing Nitem elements per thread, reducing memory transaction overhead compared to scalar copies.\n\nArguments\n\ndst: Destination GPU vector\nsrc: Source GPU vector (must have same length as dst)\nNitem=4: Number of elements processed per thread. Higher values improve throughput but require length(src) to be divisible by Nitem.\n\nExample\n\nsrc = CUDA.rand(Float32, 1024)\ndst = CUDA.zeros(Float32, 1024)\nvcopy!(dst, src)\n\nSee also: KernelForge.setvalue!\n\n\n\n\n\n","title":"KernelForge.vcopy!"},{"category":"function","location":"api/#KernelForge.setvalue!","page":"API Reference","text":"setvalue!(dst::AbstractArray{T}, val::T; Nitem=4) where T\n\nFill dst with val using vectorized GPU memory access.\n\nPerforms a high-throughput fill by storing Nitem copies of val per thread, reducing memory transaction overhead compared to scalar writes.\n\nArguments\n\ndst: Destination GPU vector\nval: Value to fill (must match element type of dst)\nNitem=4: Number of elements written per thread. Higher values improve throughput but require length(dst) to be divisible by Nitem.\n\nExample\n\ndst = CUDA.zeros(Float32, 1024)\nsetvalue!(dst, 1.0f0)\n\nSee also: KernelForge.vcopy!\n\n\n\n\n\n","title":"KernelForge.setvalue!"},{"category":"function","location":"api/#KernelForge.mapreduce","page":"API Reference","text":"mapreduce(f, op, src::AbstractArray; dims=nothing, kwargs...) -> scalar or GPU array\n\nGPU parallel map-reduce operation with optional dimension reduction.\n\nArguments\n\nf: Map function applied to each element\nop: Associative binary reduction operator\nsrc: Input GPU array\n\nKeyword Arguments\n\ndims=nothing: Dimensions to reduce over. Options:\nnothing or :: Reduce over all dimensions → scalar\nInt or Tuple{Int...}: Reduce over those dims → GPU array\ng=identity: Post-reduction transformation\ntmp=nothing: Pre-allocated buffer — a MapReduceBuffer (full reduction), VecMatBuffer (dim=1 reduction), or MatVecBuffer (dim=2 reduction)\nAdditional kwargs passed to underlying implementations\n\nFast paths\n\nFull reduction (dims=nothing) → mapreduce1d\nAll dims explicit → mapreducedims\nContiguous leading dims (1,...,k) → reshape, mapreduce2d on dim 1, reshape back\nContiguous trailing dims (k,...,n) → reshape, mapreduce2d on dim 2, reshape back\nBoth leading and trailing contiguous blocks → two mapreduce2d passes\nGeneral dims → mapreducedims fallback\n\nExamples\n\nA = CUDA.rand(Float32, 100, 50, 20)\n\n# Full reduction → scalar\ntotal = mapreduce(identity, +, A)\n\n# Reduce along dim 1: (100, 50, 20) -> (1, 50, 20)\ncol_sums = mapreduce(identity, +, A; dims=1)\n\n# Reduce along dims (1,2): (100, 50, 20) -> (1, 1, 20)\nplane_sums = mapreduce(identity, +, A; dims=(1,2))\n\n# Reduce along last dim: (100, 50, 20) -> (100, 50, 1)\ndepth_sums = mapreduce(identity, +, A; dims=3)\n\nSee also: KernelForge.mapreduce!, mapreduce1d, mapreduce2d, mapreducedims\n\n\n\n\n\nmapreduce(f, op, srcs::NTuple; kwargs...)\n\nMulti-array mapreduce. Only supports full reduction (dims=nothing).\n\nKeyword Arguments\n\ng=identity: Post-reduction transformation\ntmp=nothing: Pre-allocated MapReduceBuffer\nAdditional kwargs passed to mapreduce1d\n\n\n\n\n\n","title":"KernelForge.mapreduce"},{"category":"function","location":"api/#KernelForge.mapreduce!","page":"API Reference","text":"mapreduce!(f, op, dst, src; dims=nothing, kwargs...)\n\nIn-place GPU parallel map-reduce with dimension support.\n\nArguments\n\nf: Map function applied to each element\nop: Associative binary reduction operator\ndst: Output array\nsrc: Input GPU array\n\nKeyword Arguments\n\ndims=nothing: Dimensions to reduce over. Options:\nnothing or :: Reduce over all dimensions → writes to dst[1]\nInt or Tuple{Int...}: Reduce over those dims → writes to dst\ng=identity: Post-reduction transformation\ntmp=nothing: Pre-allocated buffer — a MapReduceBuffer (full reduction), VecMatBuffer (dim=1 reduction), or MatVecBuffer (dim=2 reduction)\nAdditional kwargs passed to underlying implementations\n\nExamples\n\nA = CUDA.rand(Float32, 100, 50)\ndst = CUDA.zeros(Float32, 1, 50)\nmapreduce!(identity, +, dst, A; dims=1)\n\nSee also: KernelForge.mapreduce\n\n\n\n\n\n","title":"KernelForge.mapreduce!"},{"category":"function","location":"api/#KernelForge.mapreduce2d","page":"API Reference","text":"mapreduce2d(f, op, src, dim; kwargs...) -> Vector\n\nGPU parallel reduction along dimension dim.\n\ndim=1: Column-wise reduction (vertical), output length = number of columns\ndim=2: Row-wise reduction (horizontal), output length = number of rows\n\nArguments\n\nf: Element-wise transformation\nop: Reduction operator\nsrc: Input matrix of size (n, p)\ndim: Dimension to reduce along (1 or 2)\n\nKeyword Arguments\n\ng=identity: Post-reduction transformation\ntmp=nothing: Pre-allocated KernelBuffer (or nothing to allocate automatically)\n\nFor dim=1 (column-wise):\n\nNitem=nothing: Items per thread\nNthreads=nothing: Threads per column reduction\nworkgroup=nothing: Workgroup size\nblocks=nothing: Number of blocks\n\nFor dim=2 (row-wise):\n\nchunksz=nothing: Chunk size for row processing\nNblocks=nothing: Number of blocks per row\nworkgroup=nothing: Workgroup size\nblocks_row=nothing: Blocks per row\n\nExamples\n\nA = CUDA.rand(Float32, 1000, 500)\n\n# Column sums (reduce along dim=1)\ncol_sums = mapreduce2d(identity, +, A, 1)\n\n# Row maximums (reduce along dim=2)\nrow_maxs = mapreduce2d(identity, max, A, 2)\n\n# Column means\ncol_means = mapreduce2d(identity, +, A, 1; g=x -> x / size(A, 1))\n\n# Sum of squares per row\nrow_ss = mapreduce2d(abs2, +, A, 2)\n\nSee also: KernelForge.mapreduce2d! for the in-place version.\n\n\n\n\n\n","title":"KernelForge.mapreduce2d"},{"category":"function","location":"api/#KernelForge.mapreduce2d!","page":"API Reference","text":"mapreduce2d!(f, op, dst, src, dim; kwargs...)\n\nIn-place GPU parallel reduction along dimension dim.\n\ndim=1: Column-wise reduction (vertical), dst length = number of columns\ndim=2: Row-wise reduction (horizontal), dst length = number of rows\n\nArguments\n\nf: Element-wise transformation\nop: Reduction operator\ndst: Output vector\nsrc: Input matrix of size (n, p)\ndim: Dimension to reduce along (1 or 2)\n\nKeyword Arguments\n\ng=identity: Post-reduction transformation\ntmp=nothing: Pre-allocated KernelBuffer (or nothing to allocate automatically)\n\nFor dim=1 (column-wise):\n\nNitem=nothing: Items per thread\nNthreads=nothing: Threads per column reduction\nworkgroup=nothing: Workgroup size\nblocks=nothing: Number of blocks\n\nFor dim=2 (row-wise):\n\nchunksz=nothing: Chunk size for row processing\nNblocks=nothing: Number of blocks per row\nworkgroup=nothing: Workgroup size\nblocks_row=nothing: Blocks per row\n\nExamples\n\nA = CUDA.rand(Float32, 1000, 500)\ncol_sums = CUDA.zeros(Float32, 500)\nrow_maxs = CUDA.zeros(Float32, 1000)\n\n# Column sums\nmapreduce2d!(identity, +, col_sums, A, 1)\n\n# Row maximums\nmapreduce2d!(identity, max, row_maxs, A, 2)\n\nSee also: KernelForge.mapreduce2d for the allocating version.\n\n\n\n\n\n","title":"KernelForge.mapreduce2d!"},{"category":"function","location":"api/#KernelForge.mapreduce1d","page":"API Reference","text":"mapreduce1d(f, op, src; kwargs...) -> scalar or GPU array\nmapreduce1d(f, op, srcs::NTuple; kwargs...) -> scalar or GPU array\n\nGPU parallel map-reduce operation.\n\nApplies f to each element, reduces with op, and optionally applies g to the final result.\n\nArguments\n\nf: Map function applied to each element\nop: Associative binary reduction operator\nsrc or srcs: Input GPU array(s)\n\nKeyword Arguments\n\ng=identity: Post-reduction transformation applied to final result\ntmp=nothing: Pre-allocated KernelBuffer (or nothing to allocate automatically)\nNitem=nothing: Items per thread (auto-selected if nothing)\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\nblocks=nothing: Number of blocks (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\nto_cpu=true: If true, return scalar; otherwise return 1-element GPU array\n\nExamples\n\n# Sum of squares\nx = CUDA.rand(Float32, 10_000)\nresult = mapreduce1d(x -> x^2, +, x)\n\n# Return GPU array instead of scalar\nresult = mapreduce1d(x -> x^2, +, x; to_cpu=false)\n\n# Dot product of two arrays\nx, y = CUDA.rand(Float32, 10_000), CUDA.rand(Float32, 10_000)\nresult = mapreduce1d((a, b) -> a * b, +, (x, y))\n\nSee also: KernelForge.mapreduce1d! for the in-place version.\n\n\n\n\n\n","title":"KernelForge.mapreduce1d"},{"category":"function","location":"api/#KernelForge.mapreduce1d!","page":"API Reference","text":"mapreduce1d!(f, op, dst, src; kwargs...)\nmapreduce1d!(f, op, dst, srcs::NTuple; kwargs...)\n\nIn-place GPU parallel map-reduce, writing result to dst[1].\n\nArguments\n\nf: Map function applied to each element\nop: Associative binary reduction operator\ndst: Output array (result written to first element)\nsrc or srcs: Input GPU array(s)\n\nKeyword Arguments\n\ng=identity: Post-reduction transformation applied to final result\ntmp=nothing: Pre-allocated KernelBuffer (or nothing to allocate automatically)\nNitem=nothing: Items per thread (auto-selected if nothing)\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\nblocks=nothing: Number of blocks (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nExamples\n\nx = CUDA.rand(Float32, 10_000)\ndst = CUDA.zeros(Float32, 1)\n\n# Sum\nmapreduce1d!(identity, +, dst, x)\n\n# With pre-allocated buffer for repeated calls\ntmp = KernelForge.get_allocation(MapReduce1D, x -> x^2, +, x)\nfor i in 1:100\n    mapreduce1d!(x -> x^2, +, dst, x; tmp)\nend\n\nSee also: KernelForge.mapreduce1d for the allocating version.\n\n\n\n\n\n","title":"KernelForge.mapreduce1d!"},{"category":"function","location":"api/#KernelForge.mapreducedims","page":"API Reference","text":"mapreducedims(f, op, src, dims; kwargs...) -> GPU array\n\nGPU parallel map-reduce over specified dimensions.\n\nApplies f to each element, reduces along dims with op, and optionally applies g to each final element.\n\nArguments\n\nf: Map function applied to each element\nop: Associative binary reduction operator\nsrc: Input GPU array\ndims: Dimension(s) to reduce over (Int or tuple of Ints)\n\nKeyword Arguments\n\ng=identity: Post-reduction transformation applied to each result element\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nExamples\n\n# Sum along rows (reduce dim 1)\nx = CUDA.rand(Float32, 128, 64)\nresult = mapreducedims(identity, +, x, 1)   # shape: (1, 64)\n\n# Sum of squares along columns (reduce dim 2)\nresult = mapreducedims(x -> x^2, +, x, 2)  # shape: (128, 1)\n\n# Reduce multiple dimensions\nx = CUDA.rand(Float32, 4, 8, 16)\nresult = mapreducedims(identity, +, x, (1, 3))  # shape: (1, 8, 1)\n\nSee also: KernelForge.mapreducedims! for the in-place version.\n\n\n\n\n\n","title":"KernelForge.mapreducedims"},{"category":"function","location":"api/#KernelForge.mapreducedims!","page":"API Reference","text":"mapreducedims!(f, op, dst, src, dims; kwargs...)\n\nIn-place GPU parallel map-reduce over specified dimensions, writing result to dst.\n\nArguments\n\nf: Map function applied to each element\nop: Associative binary reduction operator\ndst: Output array (must have size 1 along each reduced dimension)\nsrc: Input GPU array\ndims: Dimension(s) to reduce over (Int or tuple of Ints)\n\nKeyword Arguments\n\ng=identity: Post-reduction transformation applied to each result element\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nExamples\n\nx = CUDA.rand(Float32, 128, 64)\ndst = CUDA.zeros(Float32, 1, 64)\n\n# Sum along dim 1\nmapreducedims!(identity, +, dst, x, 1)\n\n# Sum of squares along dim 2 with pre-allocated dst\ndst2 = CUDA.zeros(Float32, 128, 1)\nmapreducedims!(x -> x^2, +, dst2, x, 2)\n\nSee also: KernelForge.mapreducedims for the allocating version.\n\n\n\n\n\n","title":"KernelForge.mapreducedims!"},{"category":"function","location":"api/#KernelForge.scan","page":"API Reference","text":"scan(f, op, src; kwargs...) -> GPU array\nscan(op, src; kwargs...) -> GPU array\n\nGPU parallel prefix scan (cumulative reduction) using a decoupled lookback algorithm.\n\nApplies f to each element, computes inclusive prefix scan with op, and optionally applies g to each output element.\n\nArguments\n\nf: Map function applied to each element (defaults to identity)\nop: Associative binary scan operator\nsrc: Input GPU array\n\nKeyword Arguments\n\ng=identity: Post-scan transformation applied to each output element\ntmp=nothing: Pre-allocated KernelBuffer (or nothing to allocate automatically)\nNitem=nothing: Items per thread (auto-selected if nothing)\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nExamples\n\n# Cumulative sum\nx = CUDA.rand(Float32, 10_000)\nresult = scan(+, x)\n\n# Cumulative sum of squares\nresult = scan(x -> x^2, +, x)\n\n# With post-scan transformation\nresult = scan(+, x; g = sqrt)\n\n# With pre-allocated buffer for repeated calls\ntmp = KernelForge.get_allocation(Scan1D, identity, +, x)\nresult = scan(+, x; tmp)\n\nSee also: KernelForge.scan! for the in-place version.\n\n\n\n\n\n","title":"KernelForge.scan"},{"category":"function","location":"api/#KernelForge.scan!","page":"API Reference","text":"scan!(f, op, dst, src; kwargs...)\nscan!(op, dst, src; kwargs...)\n\nIn-place GPU parallel prefix scan using a decoupled lookback algorithm.\n\nApplies f to each element, computes inclusive prefix scan with op, and optionally applies g to each output element, writing results to dst.\n\nArguments\n\nf: Map function applied to each element (defaults to identity)\nop: Associative binary scan operator\ndst: Output array for scan results\nsrc: Input GPU array\n\nKeyword Arguments\n\ng=identity: Post-scan transformation applied to each output element\ntmp=nothing: Pre-allocated KernelBuffer (or nothing to allocate automatically)\nNitem=nothing: Items per thread (auto-selected if nothing)\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nExamples\n\nx = CUDA.rand(Float32, 10_000)\ndst = similar(x)\n\n# Cumulative sum\nscan!(+, dst, x)\n\n# With pre-allocated buffer for repeated calls\ntmp = KernelForge.get_allocation(Scan1D, identity, +, x)\nfor i in 1:100\n    scan!(+, dst, x; tmp)\nend\n\nSee also: KernelForge.scan for the allocating version.\n\n\n\n\n\n","title":"KernelForge.scan!"},{"category":"function","location":"api/#KernelForge.findfirst","page":"API Reference","text":"findfirst(filtr, src; kwargs...) -> Int or CartesianIndex or nothing\n\nGPU parallel findfirst. Returns the index of the first element in src for which filtr returns true, or nothing if no such element exists. For multidimensional arrays, returns a CartesianIndex.\n\nArguments\n\nfiltr: Predicate function\nsrc: Input GPU array\n\nKeyword Arguments\n\nNitem=nothing: Items per thread (auto-selected if nothing)\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\nblocks=nothing: Number of blocks (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nExamples\n\nx = adapt(backend, rand(Float32, 10_000))\nfindfirst(>(0.99f0), x)       # returns a linear index or nothing\n\nA = adapt(backend, rand(Float32, 100, 100))\nfindfirst(>(0.99f0), A)       # returns a CartesianIndex or nothing\n\nSee also: KernelForge.findlast.\n\n\n\n\n\n","title":"KernelForge.findfirst"},{"category":"function","location":"api/#KernelForge.findlast","page":"API Reference","text":"findlast(filtr, src; kwargs...) -> Int or CartesianIndex or nothing\n\nGPU parallel findlast. Returns the index of the last element in src for which filtr returns true, or nothing if no such element exists. Implemented by reversing src and delegating to KernelForge.findfirst, so it accepts the same keyword arguments. For multidimensional arrays, returns a CartesianIndex.\n\nArguments\n\nfiltr: Predicate function\nsrc: Input GPU array\n\nKeyword Arguments\n\nNitem=nothing: Items per thread (auto-selected if nothing)\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\nblocks=nothing: Number of blocks (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nExamples\n\nx = adapt(backend, rand(Float32, 10_000))\nfindlast(>(0.99f0), x)        # returns a linear index or nothing\n\nA = adapt(backend, rand(Float32, 100, 100))\nfindlast(>(0.99f0), A)        # returns a CartesianIndex or nothing\n\nSee also: KernelForge.findfirst.\n\n\n\n\n\n","title":"KernelForge.findlast"},{"category":"function","location":"api/#KernelForge.argmax1d","page":"API Reference","text":"argmax1d(f, rel, src; kwargs...) -> Int or GPU array\nargmax1d(f, rel, srcs::NTuple; kwargs...) -> Int or GPU array\n\nGPU parallel argmax/argmin operation.\n\nApplies f to each element, finds the extremum according to rel, and returns the index of the first extremal element. Ties are broken by smallest index.\n\nArguments\n\nf: Map function applied to each element\nrel: Comparison relation (> for argmax, < for argmin)\nsrc or srcs: Input GPU array(s)\n\nKeyword Arguments\n\ntmp=nothing: Pre-allocated KernelBuffer (or nothing to allocate automatically)\nNitem=nothing: Items per thread (auto-selected if nothing)\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\nblocks=nothing: Number of blocks (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\nto_cpu=true: If true, return scalar Int; otherwise return 1-element GPU array\n\nExamples\n\nx = CUDA.rand(Float32, 10_000)\n\n# Argmax returning scalar index\nidx = argmax1d(identity, >, x)\n\n# Argmax returning 1-element GPU array\nidx_gpu = argmax1d(identity, >, x; to_cpu=false)\n\n# Argmin of absolute values\nidx = argmax1d(abs, <, x)\n\nSee also: KernelForge.argmax1d! for the in-place version.\n\n\n\n\n\n","title":"KernelForge.argmax1d"},{"category":"function","location":"api/#KernelForge.argmax1d!","page":"API Reference","text":"argmax1d!(f, rel, dst, src; kwargs...)\nargmax1d!(f, rel, dst, srcs::NTuple; kwargs...)\n\nIn-place GPU parallel argmax/argmin, writing the index to dst[1].\n\nTies are broken by smallest index.\n\nArguments\n\nf: Map function applied to each element\nrel: Comparison relation (> for argmax, < for argmin)\ndst: Output array (index written to first element)\nsrc or srcs: Input GPU array(s)\n\nKeyword Arguments\n\ntmp=nothing: Pre-allocated KernelBuffer (or nothing to allocate automatically)\nNitem=nothing: Items per thread (auto-selected if nothing)\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\nblocks=nothing: Number of blocks (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nExamples\n\nx = CUDA.rand(Float32, 10_000)\ndst = CUDA.zeros(Int, 1)\n\n# Argmax index\nargmax1d!(identity, >, dst, x)\n\n# With pre-allocated temporary for repeated calls\ntmp = KernelForge.get_allocation(Argmax1D, identity, x)\nfor i in 1:100\n    argmax1d!(identity, >, dst, x; tmp)\nend\n\nSee also: KernelForge.argmax1d for the allocating version.\n\n\n\n\n\n","title":"KernelForge.argmax1d!"},{"category":"function","location":"api/#KernelForge.argmax","page":"API Reference","text":"argmax(rel, src::AbstractArray)\n\nGPU parallel search returning the (value, index) pair of the element that is extremal according to the relation rel. Equivalent to argmax1d(identity, rel, src).\n\nArguments\n\nrel: Comparison relation (e.g. > for maximum, < for minimum)\nsrc: Input GPU array\n\nExamples\n\nx = CuArray([3f0, 1f0, 4f0, 1f0, 5f0])\nargmax(>, x)  # returns (5f0, 5)\nargmax(<, x)  # returns (1f0, 2)\n\nSee also: KernelForge.argmax1d, KernelForge.argmin.\n\n\n\n\n\nargmax(src::AbstractArray)\n\nGPU parallel argmax returning the (value, index) pair of the maximum element. Equivalent to argmax(>, src).\n\nExamples\n\nx = CuArray([3f0, 1f0, 4f0, 1f0, 5f0])\nargmax(x)  # returns (5f0, 5)\n\nSee also: KernelForge.argmin, KernelForge.argmax1d.\n\n\n\n\n\n","title":"KernelForge.argmax"},{"category":"function","location":"api/#KernelForge.argmin","page":"API Reference","text":"argmin(src::AbstractArray)\n\nGPU parallel argmin returning the (value, index) pair of the minimum element. Equivalent to argmax(<, src).\n\nExamples\n\nx = CuArray([3f0, 1f0, 4f0, 1f0, 5f0])\nargmin(x)  # returns (1f0, 2)\n\nSee also: KernelForge.argmax, KernelForge.argmax1d.\n\n\n\n\n\n","title":"KernelForge.argmin"},{"category":"function","location":"api/#KernelForge.matvec","page":"API Reference","text":"matvec([f, op,] src::AbstractMatrix, x; kwargs...) -> GPU vector\n\nGeneralized matrix-vector operation with customizable element-wise and reduction operations.\n\nComputes y[i] = g(op_j(f(src[i,j], x[j]))) for each row i, where op_j denotes reduction over columns. For standard matrix-vector multiplication, this is y[i] = sum_j(src[i,j] * x[j]). Returns a newly allocated result vector.\n\nArguments\n\nf: Binary operation applied element-wise (default: *)\nop: Reduction operation across columns (default: +)\nsrc: Input matrix\nx: Input vector, or nothing for row-wise reduction of src alone\n\nKeyword Arguments\n\ng=identity: Unary transformation applied to each reduced row\ntmp=nothing: Pre-allocated KernelBuffer (or nothing to allocate automatically)\nchunksz=nothing: Elements per thread (auto-tuned if nothing)\nNblocks=nothing: Number of thread blocks (auto-tuned if nothing)\nworkgroup=nothing: Threads per block (auto-tuned if nothing)\nblocks_row=nothing: Number of blocks used to process a single row; relevant only for wide matrices (many columns, few rows) where parallelizing across columns is beneficial. Auto-tuned if nothing.\nNitem=nothing: Number of rows loaded per thread via vectorised loads. Defaults to 1. When Nitem > 1, Nblocks must be 1 and chunksz is set to workgroup.\narch=nothing: Architecture (auto-detected from src if nothing)\n\nExamples\n\nA = CUDA.rand(Float32, 1000, 500)\nx = CUDA.rand(Float32, 500)\n\n# Standard matrix-vector multiply: y = A * x\ny = matvec(A, x)\n\n# Row-wise sum: y[i] = sum(A[i, :])\ny = matvec(A, nothing)\n\n# Row-wise maximum: y[i] = max_j(A[i, j])\ny = matvec(identity, max, A, nothing)\n\n# Softmax numerator: y[i] = sum_j(exp(A[i,j] - x[j]))\ny = matvec((a, b) -> exp(a - b), +, A, x)\n\nSee also: matvec!.\n\n\n\n\n\n","title":"KernelForge.matvec"},{"category":"function","location":"api/#KernelForge.matvec!","page":"API Reference","text":"matvec!([f, op,] dst, src::AbstractMatrix, x; kwargs...)\n\nIn-place form of matvec: writes dst[i] = g(op_j(f(src[i,j], x[j]))).\n\nExamples\n\nA = CUDA.rand(Float32, 1000, 500)\nx = CUDA.rand(Float32, 500)\ndst = CUDA.zeros(Float32, 1000)\n\n# Standard matrix-vector multiply\nmatvec!(dst, A, x)\n\n# With pre-allocated buffer for repeated calls\ntmp = KernelForge.get_allocation(MatVec, *, +, A, x)\nfor i in 1:100\n    matvec!(dst, A, x; tmp)\nend\n\nSee matvec for the full keyword-argument list.\n\n\n\n\n\n","title":"KernelForge.matvec!"},{"category":"function","location":"api/#KernelForge.vecmat","page":"API Reference","text":"vecmat([f, op,] x, src; kwargs...) -> GPU vector\n\nGPU parallel vector-matrix multiplication. Returns a newly allocated result vector.\n\nFor the standard product, vecmat(x, A) computes y[j] = sum(x[i] * A[i,j]). When x = nothing, computes column reductions: y[j] = sum(A[i,j]).\n\nArguments\n\nf=*: Element-wise transformation applied to x[i] and A[i,j] (or just A[i,j] if x=nothing)\nop=+: Reduction operator\nx: Input vector of length n (number of rows), or nothing for pure column reduction\nsrc: Input matrix of size (n, p)\n\nKeyword Arguments\n\ng=identity: Optional post-reduction transformation\ntmp=nothing: Pre-allocated KernelBuffer (or nothing to allocate automatically)\nNitem=nothing: Number of items per thread (auto-selected if nothing)\nNthreads=nothing: Number of threads per column reduction (auto-selected if nothing)\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\nblocks=nothing: Maximum number of blocks (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nExamples\n\nA = CUDA.rand(Float32, 1000, 500)\nx = CUDA.rand(Float32, 1000)\n\n# Standard vector-matrix multiply: y = x' * A\ny = vecmat(x, A)\n\n# Column-wise sum: y[j] = sum(A[:, j])\ny = vecmat(nothing, A)\n\nSee also: vecmat!.\n\n\n\n\n\n","title":"KernelForge.vecmat"},{"category":"function","location":"api/#KernelForge.vecmat!","page":"API Reference","text":"vecmat!([f, op,] dst, x, src; kwargs...)\n\nIn-place form of vecmat: writes dst[j] = g(op_i(f(x[i], src[i,j]))).\n\nExamples\n\nA = CUDA.rand(Float32, 1000, 500)\nx = CUDA.rand(Float32, 1000)\ndst = CUDA.zeros(Float32, 500)\n\n# Standard vector-matrix multiply\nvecmat!(dst, x, A)\n\n# With pre-allocated buffer for repeated calls\ntmp = KernelForge.get_allocation(VecMat, *, +, x, A)\nfor i in 1:100\n    vecmat!(dst, x, A; tmp)\nend\n\nSee vecmat for the full keyword-argument list.\n\n\n\n\n\n","title":"KernelForge.vecmat!"},{"category":"function","location":"api/#KernelForge.sort","page":"API Reference","text":"sort(src::AbstractVector; by=identity, uint_map=KernelForge.uint_map, kwargs...)\nsort(src::AbstractVector; keys=k, ...) -> (sorted_src, sorted_keys)\n\nStable LSD radix sort on the GPU. Sorts src ascending by uint_map(by(x)). Returns a new array with the sorted values; src is not modified.\n\nKeyword Arguments\n\nby=identity: Field extractor. For tuples, e.g. by=first to sort by the first element. When keys is provided, by is applied to elements of keys, not src.\nuint_map=KernelForge.uint_map: Order-preserving projection of by(x) to an unsigned integer (UInt8/16/32/64). Defaults are provided for built-in numeric types.\nkeys=nothing: Optional key array of the same length as src. When given, src and keys are sorted together using uint_map(by(keys[i])) as the sort key for src[i]. The function returns a tuple (sorted_src, sorted_keys). Both arrays are newly allocated.\ntmp=nothing: Pre-allocated KernelBuffer (or nothing to allocate automatically).\nNitem=nothing, Nchunks=nothing, workgroup=nothing, arch=nothing: tuning knobs (auto-selected if nothing).\n\nExamples\n\nx = CuArray(rand(Float64, 10_000_000))\ny = KernelForge.sort(x)\n@assert Array(y) == sort(Array(x))\n\n# Sort tuples by the first element.\npairs = CuArray([(rand(Float32), rand(UInt32)) for _ in 1:1_000_000])\nsorted_pairs = KernelForge.sort(pairs; by=first)\n\n# Sort an array of values according to a parallel key array.\nvals = CuArray(rand(Float32, 1_000_000))\nkeys = CuArray(rand(UInt32, 1_000_000))\nsorted_vals, sorted_keys = KernelForge.sort(vals; keys=keys)\n@assert issorted(Array(sorted_keys))\n\nSee also: sort! for the in-place form.\n\n\n\n\n\n","title":"KernelForge.sort"},{"category":"function","location":"api/#KernelForge.sort!","page":"API Reference","text":"sort!(dst, src; by=identity, uint_map=KernelForge.uint_map, kwargs...) -> dst\nsort!(dst, src; keys=k[, keys_dst=kd], ...) -> (dst, keys_dst)\nsort!(src; ...) -> src                                     # in-place\nsort!(src; keys=k, ...) -> (src, keys)                      # in-place + keys\n\nIn-place form of sort. The two-argument form writes sorted output into dst; dst and src may alias.\n\nThe single-argument form sorts src in place — no dst is allocated and no src → dst copy is issued. The implementation pings between src and an internal scratch buffer (in tmp) so that the final pass lands the sorted result back in src. When npasses is even (UInt16, UInt32, UInt64, Float32, Float64) the in-place form pays zero additional copies vs the algorithmic minimum; when npasses is odd or the byte-sort fast path applies (UInt8/Int8/Bool), one extra scratch ↔ src copy is needed.\n\nWhen keys is given, the function additionally returns the destination key buffer; if keys_dst is omitted, one is allocated automatically (or aliased to keys for the in-place form).\n\nSee sort for keyword arguments.\n\n\n\n\n\n","title":"KernelForge.sort!"},{"category":"function","location":"api/#KernelForge.sortperm","page":"API Reference","text":"sortperm(src::AbstractVector; by=identity, uint_map=KernelForge.uint_map,\n         IndexType=nothing, kwargs...) -> AbstractVector{IndexType}\n\nGPU sortperm: returns a permutation vector p such that src[p] is sorted ascending under uint_map(by(·)). Stable (matches Julia's stdlib sortperm). Reuses the keyval radix kernel — see sort.\n\nKeyword Arguments\n\nby=identity: Field extractor applied to elements of src (the sort keys). For tuples, e.g. by=first.\nuint_map=KernelForge.uint_map: Order-preserving projection of by(x) to an unsigned integer.\nIndexType=nothing: Element type of the returned permutation. Defaults to Int32 when length(src) ≤ typemax(Int32), else Int64. Smaller index types reduce the number of byte passes (4 for Int32, 8 for Int64).\ntmp=nothing: Pre-allocated KernelBuffer from get_allocation(Sortperm, src; ...) for hot-loop reuse.\nNitem, Nchunks, workgroup, arch: tuning knobs.\n\nExamples\n\nsrc = CuArray(rand(Float32, 1_000_000))\np = KernelForge.sortperm(src)\n@assert Array(src)[Array(p)] == sort(Array(src))\n\nSee also: sortperm!.\n\n\n\n\n\n","title":"KernelForge.sortperm"},{"category":"function","location":"api/#KernelForge.sortperm!","page":"API Reference","text":"sortperm!(perm, src; by=identity, uint_map=KernelForge.uint_map, kwargs...) -> perm\n\nIn-place form of sortperm: writes the permutation into perm (which must have integer eltype and the same length as src).\n\n\n\n\n\n","title":"KernelForge.sortperm!"},{"category":"function","location":"api/#KernelForge.sample_sort","page":"API Reference","text":"sample_sort(src; backend, lt=nothing, tmax=nothing, reverse=false) -> sorted\n\nIterative hierarchical sample sort.  No host sort, no recursion: partition into globally-ordered buckets (K1 sample → K2 oem batched sort → K3 pivot pick → K4 anchored partition) until every bucket ≤ TERMINAL, then one batched per-bucket bitonic leaf (K6).\n\nSorts by an arbitrary strict-weak-order lt (default isless/<).  The SAME comparator drives the K2 sample sort, the pivot binsearch, and the leaf, so buckets are globally ordered under lt (not raw bit order) — correct for any lt and any bitstype.  reverse is a global index mirror applied after the ascending sort (composes with any lt).\n\nLeaf padding strategy (tmax = an optional user max-sentinel, a value or a ()/Type→value function):\n\nlt===nothing && tmax===nothing && typemax(T) exists → fast path, typemax(T) pad, < comparator (byte-identical to the legacy path).\ntmax given → fast path, the sentinel pad, lt comparator.  Contract: lt(x, sentinel) must hold for every real x.\notherwise (custom lt w/o tmax, or no-typemax T w/o tmax) → validity-Bool tag leaf (no sentinel needed).  Note: for a custom lt, typemax(T) is NOT a valid sentinel unless lt agrees with bit order, so this case takes the tag path even when typemax(T) exists.\n\n\n\n\n\n","title":"KernelForge.sample_sort"},{"category":"function","location":"api/#KernelForge.sort_columns","page":"API Reference","text":"sort_columns(A::AbstractMatrix; ...) -> Matrix\nsort_columns!(A::AbstractMatrix; algorithm=:auto, by=identity,\n              uint_map=KF.uint_map, lt=nothing, tmax=nothing,\n              reverse=false, tmp=nothing, arch=nothing) -> A\n\nSort each column of A independently and in-place (sort_columns!) or into a fresh matrix (sort_columns).\n\nDispatch\n\nalgorithm=:auto (default):\n\nCustom lt/tmax/reverse=true or non-uint_map-eligible T: routed to oem_sort_columns!. Requires K ≤ 4096.\nOtherwise: picks OEM or batched LSD radix per the per-type threshold in default_sort_columns_threshold.\n\nBoth batched-radix paths bounds-check OOB tile slots and never reference typemax(T) — user-defined bitstypes with uint_map (and no typemax) work end-to-end, at any K, without falling through to OEM.\n\nalgorithm=:oem: forces OEM. K ≤ 4096. algorithm=:radix: forces batched radix. Requires uint_map-eligible T (no custom lt).\n\nWorkspace\n\nPass tmp = get_allocation(SortColumns, A) to reuse the batched-radix workspace across calls. Ignored when the chosen algorithm is OEM.\n\nExamples\n\nA = CuArray(rand(Float32, 10_000, 16))\nKF.sort_columns!(A)\n@assert Array(A) == hcat(sort.(eachcol(initial))...)\n\nA2 = CuArray(rand(Int64, 1024, 1024))\nKF.sort_columns!(A2; lt = >)   # routed to OEM (custom lt)\n\n\n\n\n\n","title":"KernelForge.sort_columns"},{"category":"function","location":"api/#KernelForge.sort_columns!","page":"API Reference","text":"sort_columns(A::AbstractMatrix; ...) -> Matrix\nsort_columns!(A::AbstractMatrix; algorithm=:auto, by=identity,\n              uint_map=KF.uint_map, lt=nothing, tmax=nothing,\n              reverse=false, tmp=nothing, arch=nothing) -> A\n\nSort each column of A independently and in-place (sort_columns!) or into a fresh matrix (sort_columns).\n\nDispatch\n\nalgorithm=:auto (default):\n\nCustom lt/tmax/reverse=true or non-uint_map-eligible T: routed to oem_sort_columns!. Requires K ≤ 4096.\nOtherwise: picks OEM or batched LSD radix per the per-type threshold in default_sort_columns_threshold.\n\nBoth batched-radix paths bounds-check OOB tile slots and never reference typemax(T) — user-defined bitstypes with uint_map (and no typemax) work end-to-end, at any K, without falling through to OEM.\n\nalgorithm=:oem: forces OEM. K ≤ 4096. algorithm=:radix: forces batched radix. Requires uint_map-eligible T (no custom lt).\n\nWorkspace\n\nPass tmp = get_allocation(SortColumns, A) to reuse the batched-radix workspace across calls. Ignored when the chosen algorithm is OEM.\n\nExamples\n\nA = CuArray(rand(Float32, 10_000, 16))\nKF.sort_columns!(A)\n@assert Array(A) == hcat(sort.(eachcol(initial))...)\n\nA2 = CuArray(rand(Int64, 1024, 1024))\nKF.sort_columns!(A2; lt = >)   # routed to OEM (custom lt)\n\n\n\n\n\n","title":"KernelForge.sort_columns!"},{"category":"module","location":"api/#KernelForge.Random","page":"API Reference","text":"KernelForge.Random\n\nCounter-based RNG (Philox4x32-10) for GPU/CPU via KernelAbstractions.\n\nElement out[i] of every fill is a deterministic function of (seed, i), so results are byte-identical across runs, vector lengths, parallel decompositions, and devices (CPU ↔ CUDA ↔ AMDGPU).\n\nQuick start\n\nusing KernelForge.Random\nimport KernelForge.Random as KFR\n\nout = CUDA.zeros(Float32, 1_000_000)\nKFR.rand!(out, UInt64(42))                       # Uniform[0,1)\nKFR.randn!(out, UInt64(42))                      # Normal(0, 1)\nKFR.rand!(out, KFR.Uniform(-1f0, 1f0), UInt64(42))\nKFR.rand!(out, KFR.Exponential(2f0), UInt64(42))\nKFR.rand!(b, KFR.Bernoulli(0.3f0), UInt64(42))   # b::AbstractVector{Bool}\n\nAdding distributions\n\nDefine one struct + three @inline methods:\n\nstruct MyDist <: KernelForge.Random.AbstractDistribution{Float32}\n    p::Float32\nend\nKernelForge.Random.uniforms_per_sample(::MyDist) = 1\nKernelForge.Random.sample_from_u32(d::MyDist, u::UInt32) = ...      # GPU fast path\nKernelForge.Random.sample_at(d::MyDist, seed, i) = ...              # CPU + tail path\n\n\n\n\n\n","title":"KernelForge.Random"},{"category":"function","location":"api/#KernelForge.Random.rand!","page":"API Reference","text":"rand!(out::AbstractVector, dist::AbstractDistribution, seed::UInt64;\n      backend = get_backend(out), workgroup = 256,\n      arch = nothing, Nitem = nothing, algo = nothing)\n\nFill out with i.i.d. samples from dist, derived from (seed, i).\n\nout[i] is a pure function of (seed, i), so the result is byte-identical across runs, vector lengths, parallel decompositions, devices, AND the choice of algorithm/Nitem.\n\nAuto-tuned per architecture:\n\nalgo  — default_random_algo(arch, Random1D, N, T) picks           StandardKernel() or PersistentKernel().\nNitem — default_nitem(arch, Random1D, N, T), samples per thread           for the standard kernel (ignored by the persistent kernel).\n\nOverride either for benchmarking; output is invariant.\n\n\n\n\n\nrand!(out::AbstractVector{<:AbstractFloat}, seed::UInt64) = rand!(out, Uniform(0, 1), seed)\n\n\n\n\n\n","title":"KernelForge.Random.rand!"},{"category":"function","location":"api/#KernelForge.Random.randn!","page":"API Reference","text":"randn!(out::AbstractVector{<:AbstractFloat}, seed::UInt64) = rand!(out, Normal(0, 1), seed)\n\n\n\n\n\n","title":"KernelForge.Random.randn!"},{"category":"function","location":"api/#KernelForge.Random.randperm!","page":"API Reference","text":"randperm!(perm::AbstractVector{<:Integer}, seed::UInt64;\n          backend = get_backend(perm), workgroup = nothing,\n          arch = nothing, keys = nothing) -> perm\n\nFill perm with a uniformly random permutation of 1:length(perm), derived from seed. perm must have integer eltype.\n\nMethod: fill a Float32 scratch vector with Uniform(0, 1) samples via rand!, then call KernelForge.sortperm! — perm ends up holding the permutation that would sort the random keys.\n\nPass keys::AbstractVector{Float32} (same backend, same length as perm) to reuse a scratch buffer across calls — useful in hot loops. Otherwise a fresh keys buffer is allocated.\n\nSame (seed, length(perm)) → same permutation on any backend.\n\n\n\n\n\n","title":"KernelForge.Random.randperm!"},{"category":"type","location":"api/#KernelForge.Random.Uniform","page":"API Reference","text":"Uniform([a, b]) <: AbstractDistribution{T}\n\nContinuous Uniform(a, b) over the half-open interval [a, b). Density 1 / (b - a) on [a, b), zero elsewhere. T is promoted from a and b and must be <: AbstractFloat (Float32 or Float64). Default constructor Uniform() yields Uniform{Float32}(0, 1).\n\n\n\n\n\n","title":"KernelForge.Random.Uniform"},{"category":"type","location":"api/#KernelForge.Random.Normal","page":"API Reference","text":"Normal([μ, σ]) <: AbstractDistribution{T}\n\nNormal(μ, σ²) — density exp(-(x-μ)²/(2σ²)) / (σ√(2π)). Sampled via Box–Muller: each (u₁, u₂) Philox pair yields TWO normals (cos- and sin-halves), so a single Philox call produces 4 Normal{Float32} samples — +30% throughput vs the cos-only variant at large N. T is promoted from μ and σ. Default Normal() yields Normal{Float32}(0, 1).\n\n\n\n\n\n","title":"KernelForge.Random.Normal"},{"category":"type","location":"api/#KernelForge.Random.Exponential","page":"API Reference","text":"Exponential(λ::Real) <: AbstractDistribution{T}\n\nExponential(λ) — density λ · exp(-λx) for x ≥ 0. Sampled via the inverse-CDF -log(1 - u) / λ (the 1 - u form avoids log(0) when the uniform draw rounds down to exactly zero). λ stored as Float32.\n\n\n\n\n\n","title":"KernelForge.Random.Exponential"},{"category":"type","location":"api/#KernelForge.Random.Bernoulli","page":"API Reference","text":"Bernoulli(p::Real) <: AbstractDistribution{Bool}\n\nBernoulli(p) — emits true with probability p, false otherwise. Samples are Bool-typed (use eltype(::Bernoulli) == Bool to allocate the destination). p is stored as Float32; pass a Float64 if you need higher precision at the cost of one extra Philox lane per sample.\n\n\n\n\n\n","title":"KernelForge.Random.Bernoulli"},{"category":"type","location":"api/#KernelForge.Random.Categorical","page":"API Reference","text":"Categorical(cum_p) <: AbstractDistribution{Int32}\n\nCategorical distribution over 1:length(cum_p). cum_p is the cumulative distribution — cum_p[k] = P(X ≤ k), must be non-decreasing, and cum_p[end] == 1.0. Samples by linear search with branchless predication (best for K ≲ 32). cum_p may be a host or device array; Adapt recurses into it so a CuArray-backed Categorical works inside the kernel.\n\n\n\n\n\n","title":"KernelForge.Random.Categorical"},{"category":"function","location":"api/#KernelForge.num_sms","page":"API Reference","text":"num_sms(backend) -> Int\n\nNumber of streaming multiprocessors (NVIDIA) / compute units (AMD) on the active device of backend. Implemented per backend in the KernelForge extensions; used by the mapreduce1d/scan autotune scripts to size hardware-relative candidate grids (e.g. blocks = K × num_sms).\n\n\n\n\n\n","title":"KernelForge.num_sms"},{"category":"function","location":"api/#KernelForge.get_allocation","page":"API Reference","text":"get_allocation(::Type{MapReduce1D}, f, op, src_or_srcs, workgroup=nothing, blocks=nothing, arch=nothing)\n\nAllocate a KernelBuffer for mapreduce1d!. Useful for repeated reductions.\n\nArguments\n\nf: Map function (used to infer intermediate eltype)\nop: Reduction operator\nsrc_or_srcs: Input GPU array or NTuple of arrays (used to determine backend and eltype)\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\nblocks=nothing: Number of blocks (must match blocks used in mapreduce1d!; auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nReturns\n\nA KernelBuffer with named fields partial and flag (flags are UInt8).\n\nExamples\n\nx = CUDA.rand(Float32, 10_000)\ntmp = KernelForge.get_allocation(MapReduce1D, x -> x^2, +, x)\ndst = CUDA.zeros(Float32, 1)\n\nfor i in 1:100\n    mapreduce1d!(x -> x^2, +, dst, x; tmp)\nend\n\n\n\n\n\nget_allocation(::Type{VecMat}, f, op, x, src, Nblocks=nothing, arch=nothing) -> KernelBuffer\n\nAllocate a KernelBuffer for vecmat!. Useful for repeated calls.\n\nArguments\n\nf: Map function (used to infer intermediate eltype)\nop: Reduction operator\nx: Input vector or nothing\nsrc: Input GPU matrix (used to determine backend and eltype)\nNblocks=nothing: Number of blocks (auto-computed if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nReturns\n\nA KernelBuffer with named fields partial and flag.\n\nExamples\n\nA = CUDA.rand(Float32, 1000, 500)\nx = CUDA.rand(Float32, 1000)\ntmp = KernelForge.get_allocation(VecMat, *, +, x, A)\ndst = CUDA.zeros(Float32, 500)\n\nfor i in 1:100\n    vecmat!(dst, x, A; tmp)\nend\n\n\n\n\n\nget_allocation(::Type{Scan1D}, f, op, src, workgroup=nothing, blocks=nothing, arch=nothing)\n\nAllocate a KernelBuffer for scan!. Useful for repeated scans.\n\nArguments\n\nf: Map function (used to infer intermediate eltype)\nop: Reduction operator\nsrc: Input GPU array (used to determine backend and eltype)\nworkgroup=nothing: Workgroup size (auto-selected if nothing)\nblocks=nothing: Number of blocks (must match blocks used in scan!; auto-computed if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nReturns\n\nA KernelBuffer whose fields depend on the aggregate type H = promote_op(f, eltype(src)):\n\npackable H (primitive, sizeof ∈ {1,2,4}, e.g. Float32/Int32): a single desc::Vector{UInt64} packed status+value tile descriptor;\notherwise (e.g. Float64, complex/tuple/struct aggregates): partial1, partial2 (eltype H) and flag (UInt8).\n\nEither way the buffer is sized for blocks tiles; pass the same tmp to scan!.\n\nExamples\n\nx = CUDA.rand(Float32, 10_000)\ntmp = KernelForge.get_allocation(Scan1D, identity, +, x)\n\ndst = similar(x)\nfor i in 1:100\n    scan!(+, dst, x; tmp)\nend\n\n\n\n\n\nget_allocation(::Type{Argmax1D}, f, src, blocks=nothing, arch=nothing)\nget_allocation(::Type{Argmax1D}, f, srcs::NTuple, blocks=nothing, arch=nothing)\n\nAllocate a KernelBuffer for argmax1d!. Useful for repeated reductions.\n\nThe intermediate type is Tuple{H, Int} where H = promote_op(f, T), tracking both value and index.\n\nArguments\n\nf: Map function (used to infer intermediate eltype)\nsrc or srcs: Input GPU array(s) (used for backend and element type)\nblocks=nothing: Number of blocks (auto-selected if nothing)\narch=nothing: Architecture (auto-detected from src if nothing)\n\nExamples\n\nx = CUDA.rand(Float32, 10_000)\ntmp = KernelForge.get_allocation(Argmax1D, identity, x)\ndst = CUDA.zeros(Int, 1)\n\nfor i in 1:100\n    argmax1d!(identity, >, dst, x; tmp)\nend\n\n\n\n\n\nget_allocation(SortColumns, A::AbstractMatrix; by=identity, uint_map=KF.uint_map)\n\nPre-allocate the workspace used by batched_radix_sort_columns!. Sized for A's shape and uint_map ∘ by's output type. Workspace fields (NamedTuple):\n\nscratch :: AbstractMatrix{T} — ping-pong buffer same shape as A.\nhist    :: AbstractArray{UInt32, 3} — (256, npasses, M) per-pass exclusive global histogram per column.\npartial1, partial2 :: AbstractArray{UInt32, 4} — (256, nblocks_per_col, M, npasses) decoupled-lookback partials.\nflag    :: AbstractArray{UInt8, 3} — (nblocks_per_col, M, npasses).\n\nFor columns short enough to use the small-K single-block path (K ≤ _batch_radix_smallK_max(T)) the multi-block buffers are sized 1 along the per-block axis — still allocated so the same workspace works for any future A of compatible shape.\n\n\n\n\n\nget_allocation(::Type{Sort1D}, src; Nitem=nothing, Nchunks=nothing,\n               workgroup=nothing, arch=nothing, by=identity,\n               uint_map=KernelForge.uint_map, keys=nothing)\n\nAllocate a KernelBuffer for sort!. Holds the global byte-histogram, the two partial buffers used by decoupled lookback, the per-block flag, and a same-size scratch ping-pong buffer.\n\nWhen keys is given, an additional scratch_keys buffer of eltype(keys) is included for the key-value sort path.\n\n\n\n\n\nget_allocation(::Type{Sortperm}, src; IndexType=nothing,\n               Nitem=nothing, Nchunks=nothing, workgroup=nothing,\n               arch=nothing, by=identity,\n               uint_map=KernelForge.uint_map) -> KernelBuffer\n\nAllocate a KernelBuffer for sortperm!. Strict superset of the keyval Sort1D buffer, with one extra keys_dst field for the throwaway final-pass sorted keys.\n\n\n\n\n\n","title":"KernelForge.get_allocation"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"This page provides practical examples demonstrating KernelForge.jl's capabilities.","title":"Examples"},{"category":"section","location":"examples/#Vectorized-Copy","page":"Examples","text":"Perform memory copies with vectorized loads and stores for improved bandwidth utilization:\n\nusing KernelForge\nusing CUDA\n\nsrc = CUDA.rand(Float32, 10^6)\ndst = similar(src)\n\n# Copy with vectorized loads and stores\nKernelForge.vcopy!(dst, src; Nitem=4)\n\n@assert dst ≈ src","title":"Vectorized Copy"},{"category":"section","location":"examples/#Custom-Types","page":"Examples","text":"KernelForge supports copying custom struct types:\n\nusing KernelForge\nusing CUDA\n\nstruct Point3D\n    x::Float32\n    y::Float32\n    z::Float32\nend\n\nn = 100_000\nsrc = CuArray([Point3D(rand(), rand(), rand()) for _ in 1:n])\ndst = similar(src)\n\nKernelForge.vcopy!(dst, src; Nitem=2)\n\n@assert all(dst .== src)","title":"Custom Types"},{"category":"section","location":"examples/#Set-Values","page":"Examples","text":"Fill an array with a constant value:\n\nusing KernelForge\nusing CUDA\n\ndst = CUDA.ones(UInt8, 100_000)\nKernelForge.setvalue!(dst, 0x00; Nitem=4)\n\n@assert all(dst .== 0x00)","title":"Set Values"},{"category":"section","location":"examples/#Map-Reduce","page":"Examples","text":"","title":"Map-Reduce"},{"category":"section","location":"examples/#Basic-Reductions","page":"Examples","text":"using KernelForge\nusing CUDA\n\nx = CUDA.rand(Float32, 10^6)\n\n# Sum\ntotal = KernelForge.mapreduce(identity, +, x)\n\n# Sum of squares\nsum_sq = KernelForge.mapreduce(abs2, +, x)\n\n# Maximum\nmax_val = KernelForge.mapreduce(identity, max, x)\n\n# Minimum\nmin_val = KernelForge.mapreduce(identity, min, x)","title":"Basic Reductions"},{"category":"section","location":"examples/#Post-Reduction-Transformation,-Example-with-KernelForge.UnitFloat8","page":"Examples","text":"Apply a function after reduction using the g parameter:\n\nusing KernelForge\nusing CUDA\n\nimport KernelForge: UnitFloat8\nx = CuArray{UnitFloat8}([rand(UnitFloat8) for _ in 1:10^6])\n\n# convert to float 32 for reduction to avoid overflow\nto_f32(x) = Float32(x)\n# then back to unitfloat8\nto_uif8(x) = UnitFloat8(x)\nsum_uif8 = KernelForge.mapreduce(to_f32, +, x; g=to_uif8)\n\n# compare the sign only :\n@assert sign(Float32(sum_uif8)) == sign(sum(Float32.(x)))\n\n# We can store only 8 bit, keep rather good precision (because of intermediate Float32 conversion) and get \n# same performance as for UInt8 addition !!!","title":"Post-Reduction Transformation, Example with KernelForge.UnitFloat8"},{"category":"section","location":"examples/#Dot-Product-and-Distance","page":"Examples","text":"Reduce multiple arrays simultaneously:\n\nusing KernelForge\nusing CUDA\n\nn = 100_000\na = CUDA.rand(Float32, n)\nb = CUDA.rand(Float32, n)\n\n# Dot product: sum(a .* b)\ndot_prod = KernelForge.mapreduce1d((x, y) -> x * y, +, (a, b))\n\n# Euclidean distance: sqrt(sum((a - b)^2))\ndistance = KernelForge.mapreduce1d((x, y) -> (x - y)^2, +, (a, b); g=sqrt)\n\n# Weighted sum: sum(w .* x)\nw = CUDA.rand(Float32, n)\nx = CUDA.rand(Float32, n)\nweighted_sum = KernelForge.mapreduce1d((wi, xi) -> wi * xi, +, (w, x))","title":"Dot Product and Distance"},{"category":"section","location":"examples/#2D-Reductions","page":"Examples","text":"Reduce along rows or columns:\n\nusing KernelForge\nusing CUDA\n\nA = CUDA.rand(Float32, 1000, 500)\n\n# Column sums (reduce along dim=1)\ncol_sums = KernelForge.mapreduce(identity, +, A; dims=1)\n@assert size(col_sums) == (500,)\n\n# Row sums (reduce along dim=2)\nrow_sums = KernelForge.mapreduce(identity, +, A; dims=2)\n@assert size(row_sums) == (1000,)\n\n# Column means\nlet u = size(A, 1) # Note that the let block is necessary for compilation of the kernel\n    g(x)::Float32 = x / u\n    col_means = KernelForge.mapreduce(identity, +, A; dims=1, g=g)\nend\n# Row-wise sum of squares\nrow_ss = KernelForge.mapreduce(abs2, +, A; dims=2)\n\n# Row maximums\nrow_max = KernelForge.mapreduce(identity, max, A; dims=2)","title":"2D Reductions"},{"category":"section","location":"examples/#Higher-Dimensional-Reductions","page":"Examples","text":"using KernelForge\nusing CUDA\n\nA = CUDA.rand(Float32, 20, 30, 40)\n\n# Reduce first dimension: (20, 30, 40) → (1, 30, 40)\nresult = KernelForge.mapreduce(identity, +, A; dims=1)\n@assert size(result) == (1, 30, 40)\n\n# Reduce first two dimensions: (20, 30, 40) → (1, 1, 40)\nresult = KernelForge.mapreduce(identity, +, A; dims=(1, 2))\n@assert size(result) == (1, 1, 40)\n\n# Reduce last dimension: (20, 30, 40) → (20, 30, 1)\nresult = KernelForge.mapreduce(identity, +, A; dims=3)\n@assert size(result) == (20, 30, 1)\n\n# Reduce last two dimensions: (20, 30, 40) → (20, 1, 1)\nresult = KernelForge.mapreduce(identity, +, A; dims=(2, 3))\n@assert size(result) == (20, 1, 1)\n\n# General reduction on non-contiguous dims: (3, 4, 5, 6, 7) → (3, 1, 5, 1, 1)\nB = CUDA.rand(Float32, 3, 4, 5, 6, 7)\nresult = KernelForge.mapreduce(identity, +, B; dims=(2, 4, 5))\n@assert size(result) == (3, 1, 5, 1, 1)","title":"Higher-Dimensional Reductions"},{"category":"section","location":"examples/#Custom-Structs","page":"Examples","text":"using KernelForge\nusing CUDA\n\nstruct Stats\n    sum::Float32\n    sum_sq::Float32\n    count::Float32\nend\n\n# Map: convert each value to Stats\nf(x) = Stats(x, x^2, 1f0)\n\n# Reduce: combine Stats\nop(a::Stats, b::Stats) = Stats(a.sum + b.sum, a.sum_sq + b.sum_sq, a.count + b.count)\n\nx = CUDA.rand(Float32, 100_000)\nresult = KernelForge.mapreduce(f, op, x)\n\nmean = result.sum / result.count\nvariance = result.sum_sq / result.count - mean^2","title":"Custom Structs"},{"category":"section","location":"examples/#Operations-on-Views","page":"Examples","text":"All KernelForge functions work transparently on GPU array views, with no copies:\n\nusing KernelForge\nusing CUDA\n\nA = CUDA.rand(Float32, 100, 200)\n\n# Reduce over a column slice\ncol_slice = view(A, :, 50:150)\nresult = KernelForge.mapreduce(identity, +, col_slice; dims=1)\n@assert size(result) == (1, 101)\n\n# Full reduction on a row slice\nrow_slice = view(A, 10:90, :)\ntotal = KernelForge.mapreduce(identity, +, row_slice)\n@assert total ≈ sum(Array(A)[10:90, :])","title":"Operations on Views"},{"category":"section","location":"examples/#Prefix-Scan","page":"Examples","text":"","title":"Prefix Scan"},{"category":"section","location":"examples/#Basic-Scans","page":"Examples","text":"using KernelForge\nusing CUDA\n\nx = CUDA.rand(Float32, 10^6)\ndst = similar(x)\n\n# Cumulative sum\nKernelForge.scan!(+, dst, x)\n@assert dst ≈ CuArray(accumulate(+, Array(x)))\n\n# Cumulative product\nKernelForge.scan!(*, dst, x)\n@assert dst ≈ CuArray(accumulate(*, Array(x)))\n\n# Cumulative maximum\nKernelForge.scan!(max, dst, x)\n@assert dst ≈ CuArray(accumulate(max, Array(x)))","title":"Basic Scans"},{"category":"section","location":"examples/#Scan-with-Map-Function","page":"Examples","text":"using KernelForge\nusing CUDA\n\nx = CUDA.rand(Float32, 10_000)\n\n# Cumulative sum of squares\nresult = KernelForge.scan(abs2, +, x)\n@assert result ≈ CuArray(accumulate(+, Array(x).^2))\n\n# Cumulative sum of absolute values\nresult = KernelForge.scan(abs, +, x)","title":"Scan with Map Function"},{"category":"section","location":"examples/#Scan-on-Views","page":"Examples","text":"using KernelForge\nusing CUDA\n\nx = CUDA.rand(Float32, 1_000)\ndst = CUDA.zeros(Float32, 1_000)\n\n# Scan over a subrange, writing into a subrange of dst\nKernelForge.scan!(+, view(dst, 3:800), view(x, 3:800))\n\n@assert Array(dst)[3:800] ≈ accumulate(+, Array(x)[3:800])\n# Elements outside the view are untouched\n@assert all(Array(dst)[1:2] .== 0f0)","title":"Scan on Views"},{"category":"section","location":"examples/#Non-Commutative-Operations:-Quaternions","page":"Examples","text":"KernelForge scan function (not mapreduce) correctly handles non-commutative operations:\n\nusing KernelForge\nusing CUDA\nusing Quaternions\n\nn = 100_000\n\n# Generate random unit quaternions\nsrc_cpu = [QuaternionF64((x ./ sqrt(sum(x.^2)))...) for x in eachcol(randn(4, n))]\nsrc = CuArray(src_cpu)\ndst = similar(src)\n\n# Quaternion multiplication is non-commutative: q1 * q2 ≠ q2 * q1\nKernelForge.scan!(*, dst, src)\n\n@assert dst ≈ CuArray(accumulate(*, src_cpu))","title":"Non-Commutative Operations: Quaternions"},{"category":"section","location":"examples/#Matrix-Vector-Operations","page":"Examples","text":"","title":"Matrix-Vector Operations"},{"category":"section","location":"examples/#Standard-Matrix-Vector-Multiply","page":"Examples","text":"using KernelForge\nusing CUDA\n\nA = CUDA.rand(Float32, 1000, 500)\nx = CUDA.rand(Float32, 500)\n\n# y = A * x\ny = KernelForge.matvec(A, x)\n@assert y ≈ A * x\n\n# In-place version\ndst = CUDA.zeros(Float32, 1000)\nKernelForge.matvec!(dst, A, x)\n@assert dst ≈ A * x","title":"Standard Matrix-Vector Multiply"},{"category":"section","location":"examples/#Row-wise-Reductions","page":"Examples","text":"using KernelForge\nusing CUDA\n\nA = CUDA.rand(Float32, 1000, 500)\n\n# Row sums: y[i] = sum(A[i, :])\nrow_sums = KernelForge.matvec(A, nothing)\n@assert row_sums ≈ vec(sum(A; dims=2))\n\n# Row maximums: y[i] = max_j(A[i, j])\nrow_max = KernelForge.matvec(identity, max, A, nothing)\n@assert row_max ≈ vec(maximum(A; dims=2))\n\n# Row minimums\nrow_min = KernelForge.matvec(identity, min, A, nothing)\n@assert row_min ≈ vec(minimum(A; dims=2))","title":"Row-wise Reductions"},{"category":"section","location":"examples/#Custom-Operations","page":"Examples","text":"using KernelForge\nusing CUDA\n\nA = CUDA.rand(Float32, 1000, 500)\nx = CUDA.rand(Float32, 500)\n\n# Softmax numerator: y[i] = sum_j(exp(A[i,j] - x[j]))\ny = KernelForge.matvec((a, b) -> exp(a - b), +, A, x)\n\n# Row-wise dot product with scaling: y[i] = sqrt(sum_j(A[i,j]^2 * x[j]^2))\ny = KernelForge.matvec((a, b) -> a^2 * b^2, +, A, x; g=sqrt)","title":"Custom Operations"},{"category":"section","location":"examples/#Custom-Struct-Output","page":"Examples","text":"using KernelForge\nusing CUDA\n\nstruct Vec3\n    x::Float32\n    y::Float32\n    z::Float32\nend\n\n# Map: combine matrix and vector elements\nf(a, b) = Vec3(a * b, a + b, a - b)\n\n# Reduce: component-wise sum\nop(v1::Vec3, v2::Vec3) = Vec3(v1.x + v2.x, v1.y + v2.y, v1.z + v2.z)\n\nA = CUDA.rand(Float32, 200, 500)\nx = CUDA.rand(Float32, 500)\ndst = CuArray{Vec3}(undef, 200)\n\nKernelForge.matvec!(f, op, dst, A, x)","title":"Custom Struct Output"},{"category":"section","location":"examples/#Matvec-on-Views","page":"Examples","text":"using KernelForge\nusing CUDA\n\nA = CUDA.rand(Float32, 100, 200)\nx = CUDA.rand(Float32, 200)\n\n# Multiply using only a submatrix and a subvector\nresult = KernelForge.matvec(view(A, 10:90, 50:150), view(x, 50:150))\n@assert Array(result) ≈ Array(A)[10:90, 50:150] * Array(x)[50:150]","title":"Matvec on Views"},{"category":"section","location":"examples/#Vector-Matrix-Operations","page":"Examples","text":"","title":"Vector-Matrix Operations"},{"category":"section","location":"examples/#Standard-Vector-Matrix-Multiply","page":"Examples","text":"using KernelForge\nusing CUDA\n\nA = CUDA.rand(Float32, 1000, 500)\nx = CUDA.rand(Float32, 1000)\n\n# y = x' * A (column-wise weighted sum)\ndst = CUDA.zeros(Float32, 500)\nKernelForge.vecmat!(dst, x, A)\n@assert dst ≈ vec(x' * A)","title":"Standard Vector-Matrix Multiply"},{"category":"section","location":"examples/#Column-wise-Reductions","page":"Examples","text":"using KernelForge\nusing CUDA\n\nA = CUDA.rand(Float32, 1000, 500)\n\n# Column sums: y[j] = sum(A[:, j])\ndst = CUDA.zeros(Float32, 500)\nKernelForge.vecmat!(dst, nothing, A)\n@assert dst ≈ vec(sum(A; dims=1))","title":"Column-wise Reductions"},{"category":"section","location":"examples/#Search","page":"Examples","text":"","title":"Search"},{"category":"section","location":"examples/#findfirst","page":"Examples","text":"Find the index of the first element satisfying a predicate:\n\nusing KernelForge\nusing CUDA\n\nx = CUDA.rand(Float32, 100_000)\n\n# First element above threshold\ni = KernelForge.findfirst(>(0.999f0), x)\n# i is either an Int index or nothing\n\n# First exact match\nCUDA.@allowscalar x[42] = -1f0\ni = KernelForge.findfirst(==(-1f0), x)\n@assert i == 42","title":"findfirst"},{"category":"section","location":"examples/#findlast","page":"Examples","text":"using KernelForge\nusing CUDA\n\nx = CUDA.rand(Float32, 100_000)\nCUDA.@allowscalar x[99_000] = -1f0\ni = KernelForge.findlast(==(-1f0), x)\n@assert i == 99_000","title":"findlast"},{"category":"section","location":"examples/#findfirst-on-a-2D-array","page":"Examples","text":"For multidimensional arrays, a CartesianIndex is returned:\n\nusing KernelForge\nusing CUDA\n\nA = CUDA.rand(Float32, 100, 200)\nCUDA.@allowscalar A[37, 82] = -1f0\n\nidx = KernelForge.findfirst(==(-1f0), A)\n@assert idx == CartesianIndex(37, 82)","title":"findfirst on a 2D array"},{"category":"section","location":"examples/#findfirst-on-custom-structs","page":"Examples","text":"using KernelForge\nusing CUDA\n\nstruct Particle\n    mass::Float32\n    charge::Float32\nend\n\nparticles = CuArray([Particle(rand(), rand() - 0.5f0) for _ in 1:10_000])\n\n# First negatively charged particle\ni = KernelForge.findfirst(p -> p.charge < 0f0, particles)","title":"findfirst on custom structs"},{"category":"section","location":"examples/#argmax-/-argmin","page":"Examples","text":"","title":"argmax / argmin"},{"category":"section","location":"examples/#Basic-usage","page":"Examples","text":"argmax and argmin return the index of the extremal element:\n\nusing KernelForge\nusing CUDA\n\nx = CUDA.rand(Float32, 100_000)\n\ni = KernelForge.argmax(x)   # index of maximum\nj = KernelForge.argmin(x)   # index of minimum\n\n@assert Array(x)[i] == maximum(Array(x))\n@assert Array(x)[j] == minimum(Array(x))","title":"Basic usage"},{"category":"section","location":"examples/#Custom-relation","page":"Examples","text":"Pass any binary relation to find the extremum under a custom ordering:\n\nusing KernelForge\nusing CUDA\n\nx = CUDA.rand(Float32, 100_000)\n\n# Equivalent to argmax\ni = KernelForge.argmax(>, x)\n\n# Equivalent to argmin\nj = KernelForge.argmax(<, x)","title":"Custom relation"},{"category":"section","location":"examples/#argmax-with-custom-structs","page":"Examples","text":"Use argmax1d directly with a map function f and a relation rel for full control:\n\nusing KernelForge\nusing CUDA\n\nstruct PriorityItem\n    priority::Int32\n    cost::Float32\nend\n\n# \"better\" means higher priority; ties broken by lower cost\nbetter(a::PriorityItem, b::PriorityItem) =\n    a.priority > b.priority || (a.priority == b.priority && a.cost < b.cost)\n\nitems = CuArray([PriorityItem(rand(Int32(1):Int32(5)), rand(Float32)) for _ in 1:10_000])\n\n# Index of the best item\ni = KernelForge.argmax(better, items)","title":"argmax with custom structs"},{"category":"section","location":"examples/#Tie-breaking","page":"Examples","text":"When multiple elements share the extremal value, the first index is always returned:\n\nusing KernelForge\nusing CUDA\n\nx = CuArray(Float32[5, 5, 5, 5, 5])\n@assert KernelForge.argmax(x) == 1\n@assert KernelForge.argmin(x) == 1","title":"Tie-breaking"},{"category":"section","location":"examples/#Sort","page":"Examples","text":"KernelForge ships four sort variants. The right choice depends on what you have and what you need:\n\nAPI Use when\nsort / sort! 1D radix on BitwiseType (Ints, Floats, Bool, UnitFloat8). Fastest path.\nsort!(src; keys=vals) Keyval radix — reorder vals in parallel with src. Use for key/value pairs.\nsortperm / sortperm! You want the permutation indices, not the sorted values.\nsample_sort General lt comparator (custom orderings, structs, non-radix types).\nsort_columns / sort_columns! Sort each column of a K × M matrix in parallel.","title":"Sort"},{"category":"section","location":"examples/#1D-radix-sort","page":"Examples","text":"using KernelForge\nusing CUDA\n\nsrc = CUDA.rand(Float32, 10^6)\n\n# In-place — fastest; no extra copy when the radix pass count is odd\nKernelForge.sort!(src)\n\n# Out-of-place — leaves src untouched\nsrc   = CUDA.rand(Float32, 10^6)\ndst   = similar(src)\nKernelForge.sort!(dst, src)\n\n# Allocate + return — convenience form\nout = KernelForge.sort(CUDA.rand(Float32, 10^6))","title":"1D radix sort"},{"category":"section","location":"examples/#Keyval-sort","page":"Examples","text":"Sort vals by the corresponding entries of keys — handy for ordering payloads (e.g., particle indices) by a per-element score:\n\nusing KernelForge, CUDA\n\nscores  = CUDA.rand(Float32, 10^6)\nindices = CuArray(UInt32.(1:10^6))\n\nKernelForge.sort!(scores; keys=indices)\n# indices is now reordered so that scores[i] is non-decreasing","title":"Keyval sort"},{"category":"section","location":"examples/#Sortperm","page":"Examples","text":"When you want the permutation rather than the sorted values:\n\nusing KernelForge, CUDA\n\nsrc  = CUDA.rand(Float32, 10^6)\nperm = KernelForge.sortperm(src)\n# src[perm] is sorted\n\n# In-place form\nperm = similar(src, UInt32)\nKernelForge.sortperm!(perm, src)","title":"Sortperm"},{"category":"section","location":"examples/#General-comparator-sample-sort","page":"Examples","text":"sample_sort accepts an arbitrary lt comparator and works on any bitstype — including types the radix path can't handle (e.g., tuples, custom structs, reverse order):\n\nusing KernelForge, CUDA\n\nsrc = CUDA.rand(Float32, 10^6)\n\n# Descending order\nout = KernelForge.sample_sort(src; lt = >)\n\n# Sort tuples by their 2nd element\npairs = CuArray([(Float32(i), Float32(rand())) for i in 1:10^5])\nsorted = KernelForge.sample_sort(pairs; lt = (a, b) -> a[2] < b[2])","title":"General-comparator sample sort"},{"category":"section","location":"examples/#Batched-per-column-sort","page":"Examples","text":"sort_columns! sorts each column of a K × M matrix in parallel — the workhorse for batched-prefix-work over column-major data:\n\nusing KernelForge, CUDA\n\nA = CUDA.rand(Float32, 4096, 64)          # 64 vectors of length 4096\nKernelForge.sort_columns!(A)\n# Each column A[:, k] is now sorted independently","title":"Batched per-column sort"},{"category":"section","location":"examples/#Random","page":"Examples","text":"KernelForge.Random is a counter-based RNG: out[i] is a pure function of (seed, i), so results are byte-identical across runs, vector lengths, and devices (CPU ↔ CUDA ↔ AMDGPU). There is no mutable RNG state object — the seed (a UInt64) is everything you need.","title":"Random"},{"category":"section","location":"examples/#Uniform-draws","page":"Examples","text":"import KernelForge.Random as KFR\nusing CUDA\n\nout = CUDA.zeros(Float32, 10^6)\n\nKFR.rand!(out, UInt64(42))                       # Uniform[0, 1)\nKFR.rand!(out, KFR.Uniform(-1f0, 1f0), UInt64(42))  # Uniform[-1, 1)","title":"Uniform draws"},{"category":"section","location":"examples/#Normal-draws","page":"Examples","text":"KFR.randn!(out, UInt64(42))                      # Normal(0, 1)\nKFR.rand!(out, KFR.Normal(2.0f0, 0.5f0), UInt64(42))  # Normal(2, 0.25)","title":"Normal draws"},{"category":"section","location":"examples/#Other-distributions","page":"Examples","text":"# Exponential(λ = 2)\nKFR.rand!(out, KFR.Exponential(2f0), UInt64(42))\n\n# Bernoulli — emits Bool vector\nb = CUDA.zeros(Bool, 10^6)\nKFR.rand!(b, KFR.Bernoulli(0.3f0), UInt64(42))\n\n# Categorical — cum_p is the cumulative CDF (last entry == 1)\ncum_p = cu(Float32[0.2, 0.5, 1.0])               # P = [0.2, 0.3, 0.5]\nlabels = CUDA.zeros(Int32, 10^6)\nKFR.rand!(labels, KFR.Categorical(cum_p), UInt64(42))","title":"Other distributions"},{"category":"section","location":"examples/#Reproducibility","page":"Examples","text":"Reusing the same (seed, length) always yields the same draws — useful when you want byte-equal results across runs or to debug divergent behaviour:\n\na = CUDA.zeros(Float32, 1000); KFR.rand!(a, UInt64(7))\nb = CUDA.zeros(Float32, 1000); KFR.rand!(b, UInt64(7))\n@assert Array(a) == Array(b)","title":"Reproducibility"},{"category":"section","location":"examples/#Random-permutations","page":"Examples","text":"randperm! populates a Float32 key buffer with uniforms and routes through sortperm! from the sort family:\n\nperm = CUDA.zeros(UInt32, 10^6)\nKFR.randperm!(perm, UInt64(42))\n# perm is now a uniform random permutation of 1:10^6","title":"Random permutations"},{"category":"section","location":"examples/#Pre-allocating-Temporary-Buffers","page":"Examples","text":"For repeated operations, pre-allocate temporary buffers to avoid allocation overhead:\n\nusing KernelForge\nusing CUDA\n\nx = CUDA.rand(Float32, 100_000)\ndst = similar(x)\n\n# Pre-allocate for scan\ntmp = KernelForge.get_allocation(KernelForge.Scan1D, identity, +, x)\n\n# Reuse in a loop\nfor i in 1:100\n    CUDA.rand!(x)  # new random data\n    KernelForge.scan!(+, dst, x; tmp=tmp)\nend","title":"Pre-allocating Temporary Buffers"},{"category":"section","location":"examples/#Complete-Example:-Online-Statistics","page":"Examples","text":"Compute running mean and variance in a single pass:\n\nusing KernelForge\nusing CUDA\n\nstruct RunningStats\n    n::Float32\n    mean::Float32\n    m2::Float32  # sum of squared deviations\nend\n\n# Welford's online algorithm for combining statistics\nfunction combine(a::RunningStats, b::RunningStats)\n    n = a.n + b.n\n    delta = b.mean - a.mean\n    mean = a.mean + delta * b.n / n\n    m2 = a.m2 + b.m2 + delta^2 * a.n * b.n / n\n    return RunningStats(n, mean, m2)\nend\n\n# Initialize each element as a single observation\ninit(x) = RunningStats(1f0, x, 0f0)\n\nx = CUDA.rand(Float32, 1_000_000)\ndst = CuArray{RunningStats}(undef, length(x))\n\nKernelForge.scan!(init, combine, dst, x)\n\n# Final statistics\nfinal_stats = Array(dst)[end]\nmean = final_stats.mean\nvariance = final_stats.m2 / final_stats.n\nstd = sqrt(variance)","title":"Complete Example: Online Statistics"},{"category":"section","location":"#KernelForge.jl","page":"Home","text":"High-performance, portable GPU primitives for Julia. A pure Julia implementation delivering performance competitive with optimized CUDA C++ libraries.\n\nwarning: Experimental Status\nThis package is in an experimental phase. Although extensive testing has been performed, no bounds checking is performed, which may lead to unexpected behavior with out-of-bounds access. Correctness and performance have been validated on NVIDIA (RTX 1000, A40) and AMD (MI300X) GPUs.\n\ninfo: Architecture & Contributions\nKernelForge.jl builds on KernelAbstractions.jl for GPU kernel dispatch. However, certain low-level operations—including warp shuffle instructions, vectorized memory access, and memory ordering semantics—are not yet available in KA.jl, so we use KernelIntrinsics.jl for these primitives. The core contribution of this package lies in the GPU kernel implementations themselves, designed to be portable across GPU backends. CUDA and AMDGPU are both supported as weak dependencies; Intel GPU support would primarily require work in KernelIntrinsics.jl.\n\nnote: Citation\nA paper describing this work is in preparation. If you use this code, please check back for citation details.","title":"KernelForge.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"KernelForge\")","title":"Installation"},{"category":"section","location":"#Features","page":"Home","text":"Map-reduce with custom functions and operators, supporting arbitrary dimensions and multidimensional arrays including non-contiguous dimension reduction via mapreducedims\nPrefix scan supporting non-commutative operations\nMatrix-vector operations with customizable element-wise and reduction operations\nSort — 1D radix sort/sort! with optional keys= keyval form, sortperm for permutation indices, sample_sort accepting an arbitrary lt comparator, and sort_columns for batched per-column sort of K × M matrices\nRandom — Philox4x32-10 counter-based GPU RNG (deterministic across CPU and GPU for a given seed); rand!, randn!, randperm!, plus Uniform, Normal, Exponential, Bernoulli, Categorical distribution drivers\nSearch — findfirst, findlast, argmax, argmin on GPU arrays\nVectorized copy with configurable load/store widths\nPer-arch tuning — runtime lookup of pre-tuned kernel parameters from data/tuning/<Arch>.{json,jl}; graceful fallback to heuristic defaults when no tuning ships for the detected arch\nViews and strided arrays supported throughout, enabled by KernelIntrinsics.jl's vectorized memory access primitives which correctly handle non-contiguous memory layouts\nSupports CUDA (NVIDIA) and AMDGPU (AMD) backends via weak dependencies\nIncludes UnitFloat8, a custom 8-bit floating-point type with range (-1, 1) for testing","title":"Features"},{"category":"section","location":"#Quick-Start","page":"Home","text":"using KernelForge\nusing CUDA\n\n# Prefix scan\nsrc = CUDA.rand(Float32, 10^6)\ndst = similar(src)\nKernelForge.scan!(+, dst, src)\n\n# Matrix-vector multiply\nA = CUDA.rand(Float32, 1000, 500)\nx = CUDA.rand(Float32, 500)\ny = KernelForge.matvec(A, x)\n\n# Map-reduce (full reduction)\ntotal = KernelForge.mapreduce(abs2, +, src)\n\n# Map-reduce over specific dimensions\nB = CUDA.rand(Float32, 4, 8, 16)\nresult = KernelForge.mapreduce(identity, +, B; dims=(1, 3))  # shape: (1, 8, 1)\n\n# Views are supported\nv = view(src, 1:2:10^6)\ntotal_view = KernelForge.mapreduce(abs2, +, v)\n\n# Search\ni = KernelForge.findfirst(>(0.99f0), src)\nj = KernelForge.argmax(src)\n\n# Sort (1D radix; ~CUB throughput on Turing)\nkeys = CUDA.rand(Float32, 10^6)\nKernelForge.sort!(keys)                              # in-place\nperm = KernelForge.sortperm(CUDA.rand(Float32, 10^6))  # permutation indices\n\n# Random — counter-based RNG; (seed, i) → byte-identical CPU ↔ CUDA ↔ AMDGPU\nimport KernelForge.Random as KFR\ndraws = CUDA.zeros(Float32, 10^6)\nKFR.rand!(draws, UInt64(42))                          # Uniform[0, 1)\nKFR.randn!(draws, UInt64(42))                         # Normal(0, 1)\nKFR.rand!(draws, KFR.Exponential(2f0), UInt64(42))    # Exponential(2)","title":"Quick Start"},{"category":"section","location":"#Acknowledgments","page":"Home","text":"This package builds on the foundation provided by KernelAbstractions.jl and CUDA.jl. The API design draws inspiration from several packages in the Julia ecosystem. Development of the API and documentation was assisted by Claude (Anthropic).","title":"Acknowledgments"},{"category":"section","location":"#Sponsors","page":"Home","text":"KernelForge.jl is an open-source project maintained on personal time. If it is useful to you — especially in a production or HPC setting — you can support its development via GitHub Sponsors. Corporate sponsors receive priority support on issues and an acknowledgment here in the documentation.","title":"Sponsors"},{"category":"section","location":"#License","page":"Home","text":"MIT","title":"License"}]
}
